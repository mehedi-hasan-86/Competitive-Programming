#include<bits/stdc++.h>
using namespace std;
#define ull unsigned long long
#define lli long long int
#define endl "\n"
#define sz(x) (int)(x).size()
using vl = vector<lli>;
using vi = vector<int>;
const int MAX = 1e6 + 5;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    // Just only where we declare and what is the best
    /*
    1. if we declare array in local that is worst case  because
     only 1 million can store 
    2. if we declare array in globally that is like 
    static declaration which can store 10-100 million
    3. if we declare vector which is like a heap
    so vector is best for declare ok 
    which can store 10-100 million

    */

    return 0;
}